{
  "compilerOptions": {
    "baseUrl": ".", // Указывает на корень проекта
    "paths": {
      "@/*": ["./src/*"] // Настраивает алиасы
    }
  },
  "exclude": ["node_modules"] // Исключаем папку node_modules из обработки
}
